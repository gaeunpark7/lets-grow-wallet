import 'package:flutter/material.dart';

class CalendarDetailRow extends StatelessWidget {
  const CalendarDetailRow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
